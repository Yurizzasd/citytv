name = "citytv"
compatibility_date = "2024-09-01"

[vars]
# Troque pela URL real da sua Anivexa self-hosted via secret (nunca commite aqui):
#   wrangler secret put ANIVEXA_BASE_URL
# ANIVEXA_BASE_URL = "https://sua-api.example.com"

[build]
command = "npm run build"
publish = "dist"
